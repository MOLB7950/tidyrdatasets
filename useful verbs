Useful verbs for tidyr, dplyr

setNames() = setNames(c("x","y"")   sets the names of columns

select() = select("x","y") or select (1,2,3)  selects columns by number or "name"

mutate() = adds a new column == mutate(newcolum = XYZ)

gather()

